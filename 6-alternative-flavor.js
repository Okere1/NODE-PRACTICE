module.exports.items = ['item1','item2'] // exporting a property which contains an array
const person = {
    name: 'bob'
}

module.exports.singlePerson = person // exporting a property with has an object