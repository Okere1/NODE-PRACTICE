const sayHi = (name)=>{  // another way of writing functions
    console.log(`Hello there ${name}`) // we use `${}`  when adding variables to an output string
    }

    module.exports = sayHi  // exporting a funtion

    